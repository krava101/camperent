export const selectCurrentAdvert = state => state.currentAdvert.advert;

export const selectCategories = state => state.currentAdvert.categories;

export const selectReviews = state => state.currentAdvert.advert.reviews;
